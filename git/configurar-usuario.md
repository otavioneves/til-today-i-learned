Para configurar o usuário no git, usa-se o `git config` com o argumento `--local`, como por exemplo:

```
git config --local user.name "Otavio Neves"
git config --local user.email "email@outlook.com"
```
